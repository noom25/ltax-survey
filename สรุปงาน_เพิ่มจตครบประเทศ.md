# สรุปงานระบบ LTAX Survey — รอบเพิ่ม จังหวัด/อำเภอ/ตำบล ครบทั้งประเทศ (19/08/2569)

> สรุปงานรอบนี้: dropdown จ-อ-ต ครบ 77 จังหวัด / 928 อำเภอ / 7,436 ตำบล ในฟอร์มเจ้าของ/ที่ดิน/ป้าย
> (เดิมฟอร์มมีแค่ข้อมูล อ.ขุขันธ์ + เมืองศรีสะเกษ)

---

## 1. งานที่ทำรอบนี้ (หลัก)

### ✅ สร้างข้อมูลกลาง จ-อ-ต ครบประเทศ (`admin_data.js`)
- **แหล่งข้อมูล:** `thailand-geography-data/thailand-geography-json` (raw.githubusercontent.com)
  - `geo_src\provinces.json` (77 จังหวัด) / `districts.json` (928 อำเภอ) / `subdistricts.json` (7,436 ตำบล)
- **สร้างจาก:** `geo_src\build_admin_data.py` → `admin_data.js` (255,029 bytes)
- **โครงสร้าง:**
  ```js
  window.ADMIN_DATA = {
    provinces:   [{c:'33', n:'ศรีสะเกษ'}, ...],            // 77 ตัว
    byProvince:  {'33': [{c:'3305', n:'ขุขันธ์'}, ...], ...}, // 77 keys (อำเภอตามจังหวัด)
    byDistrict:  {'3305': [{c:'330506', n:'ปรือใหญ่'}, ...], ...} // 928 keys (ตำบลตามอำเภอ)
  };
  ```
- **ตรวจความถูกต้อง:** 330506=ปรือใหญ่ / 3305=ขุขันธ์ / 33=ศรีสะเกษ / 66=พิจิตร / s33 อำเภอ=22 / s3305 ตำบล=22 — ตรงระบบ DLA

### ✅ ฟอร์ม 3 ตัว ใช้ `ADMIN_DATA` (pattern เดียวกับฟอร์มเจ้าของที่ผู้ใช้สั่งให้เป็นต้นแบบ)
- **ฟอร์ม_เพิ่มเจ้าของทรัพย์สิน.html** (เคยเป็น PROVINCES 75 จ. + ADMIN เฉพาะ ศก. → เปลี่ยนเป็น 77 จ. ครบ)
  - init ตั้ง default **ขุขันธ์/ปรือใหญ่ (3305/330506)** แทนตัวแรก (เมืองศรีสะเกษ)
  - loadEditRecord: ตั้ง จ → เติม อ → ตั้ง อ → เติม ต → ตั้ง ต เป็นลำดับ (ไม่พังเหมือน setSelectOrAdd เดิม)
- **ฟอร์ม_เพิ่มที่ดิน.html** (เดิมไม่มี dropdown จังหวัด!)
  - เพิ่ม `<select id="provinceId">` เหนืออำเภอ
  - collect() เก็บ `'จังหวัด'` จาก select (ไม่ hardcode '33')
  - init/resetForm/loadEditRecord อัปเดตครบ + REQUIRED เพิ่ม `provinceId`
- **ฟอร์ม_เพิ่มป้าย.html** (เดิมอำเภอฝัง option เดียว "ขุขันธ์" + DISTRICTS 28 hardcode)
  - เปลี่ยนเป็น dropdown จ อ ต เต็ม; เก็บทั้งชื่ออ/ต (ส่ง LTAX) + `รหัสจังหวัด/รหัสอำเภอ/รหัสตำบล` (ไว้อ่านกลับตอนแก้ไข)
  - saveSign อ่านค่าจริง + REQUIRED เพิ่ม `amphurId`, `provinceId`

### ✅ build_pwa.py + copy ขึ้น web\
- เพิ่ม `<script src="admin_data.js"></script>` หลัง `</style>` + เพิ่ม `admin_data.js` ใน sw cache (assets)
- copy ฟอร์มที่แก้ + admin_data.js → `web\` แล้วรัน build: **index.html 256,959 bytes (7 ฟอร์มฝัง)**

### ✅ ทดสอบจริงผ่าน (Selenium CDP 9222, จาก `C:\Users\NOOM\AppData\Local\Temp\opencode\test_geo_admin.py`)
| ฟอร์ม | จังหวัด opts | เลือก | อ/ต | ผลบันทึก |
|---|---|---|---|---|
| ที่ดิน | 78 (77+ตัวเลือก) | เชียงใหม่ 50 → เมืองเชียงใหม่ 5001 | 17 ต. → 500101 | ✅ เก็บ {50, 5001, 500101} |
| ป้าย | 78 | ลำพูน 51 → เมืองลำพูน 5101 | 16 ต. → 510101 | ✅ เก็บ {51, 5101, 510101} + ชื่ออ/ต |
| เจ้าของ | 77 | พิษณุโลก 65 → เมืองพิษณุโลก 6501 | 21 ต. → 650101 | ✅ JS errors 0 |

### ✅ Deploy GitHub Pages ใหม่
- commit `0d63b156` (15 ไฟล์: เดิม 14 + admin_data.js) — refs/heads/main อัปเดต
- ตรวจเว็บจริง: `admin_data.js` 200/255,029 B/77 จังหวัด + index.html มี provinceId ครบ 3 ฟอร์ม
- **URL:** https://noom25.github.io/ltax-survey/

---

## 2. วิธี deploy (รอบนี้ใช้ gh api ไม่มี git CLI)
```powershell
# 1) สคริปต์พร้อมใช้ (แก้ commit message ได้): C:\Users\NOOM\AppData\Local\Temp\opencode\deploy_ltax.ps1
powershell -ExecutionPolicy Bypass -File deploy_ltax.ps1
#    ทำ: blobs (base64, BODY ห้ามมี BOM!) → tree → commit → PATCH refs/heads/main
#    อัปโหลดทุกไฟล์ใน web\ recursive อัตโนมัติ (รวม admin_data.js/icons/sw)
```

---

## 3. สถานะโปรเจกต์รวม (สะสม)

| งาน | สถานะ |
|---|---|
| ระบบ v1 ครบ: ฟอร์มมือถือ 7 + server + ส่งข้อมูล + แปลง Excel/รูป + บอทอัป LTAX | ✅ เสร็จ |
| ค้นหา/ดู/แก้ไข/ลบข้อมูลบนหน้าแรก | ✅ เสร็จ |
| PWA เต็มรูปแบบ (manifest + sw + ไอคอน ติดตั้งเป็นแอป ออฟไลน์ 100%) | ✅ เสร็จ |
| SPA หน้าเดียว (build_pwa.py) + หน้าถ่ายรูปชุด + รวมข้อมูลแนบรูป | ✅ เสร็จ |
| ดูข้อมูล (viewData) แบบ AST001 + ปุ่มแก้ไขแยก | ✅ เสร็จ |
| theme LTAX Online (AdminLTE กรม) + json_to_excel ครบ 6 ชีต | ✅ เสร็จ |
| **dropdown จ-อ-ต ครบ 77 จ. (เจ้าของ/ที่ดิน/ป้าย)** | ✅ เสร็จ (รอบนี้) |
| Deploy GitHub Pages | ✅ เสร็จ (commit 0d63b15) |
| ทดสอบใช้งานจริงบนมือถือ (iPhone) | ⬜ ยัง |
| รันบอทอัปจริงกับ LTAX Online (login+OTP) | ⬜ ยัง |
| อัปเดต คู่มือการใช้งาน.md ให้ครอบคลุม PWA + ถ่ายรูป + ดูข้อมูล + จ-อ-ต | ⬜ ยัง |

---

## 4. ไฟล์ที่แก้/สร้าง รอบนี้

| ไฟล์ | การเปลี่ยนแปลง |
|---|---|
| `admin_data.js` | **ใหม่** — ข้อมูล จ/อ/ต ครบ 77/928/7,436 (window.ADMIN_DATA) สร้างจาก geo_src |
| `geo_src\build_admin_data.py` + `*.json` | **ใหม่** — สคริปต์/ข้อมูลต้นทาง thailand-geography-data |
| `ฟอร์ม_เพิ่มเจ้าของทรัพย์สิน.html` | 77 จ. ครบ + init ขุขันธ์/ปรือใหญ่ + loadEditRecord เป็นลำดับ |
| `ฟอร์ม_เพิ่มที่ดิน.html` | เพิ่ม dropdown จังหวัด + ADMIN_DATA + collect/init/reset/loadEdit |
| `ฟอร์ม_เพิ่มป้าย.html` | จ อ ต เต็ม + เก็บรหัส จ/อ/ต + saveSign dynamic |
| `build_pwa.py` | เพิ่ม script admin_data.js + asset ใน sw |
| `web\` (index.html 256,959 B, sw.js v20260819-185632, admin_data.js) | build/คัดลอกใหม่ + deploy |
| สคริปต์ทดสอบ (Temp\opencode) | `test_geo_admin.py` (ผ่าน) + `debug_spa.py` |
| `deploy_ltax.ps1` (Temp\opencode) | deploy ผ่าน gh api (ไม่มี BOM) |

---

## 5. ข้อควรจำ/ข้อควรระวัง
- **ห้ามแก้ `web\index.html` ตรง** — เกิดจาก `python build_pwa.py` (แก้ที่ ฟอร์ม_*.html / home.html แล้ว build)
- **แก้ฟอร์มแล้วต้อง:** copy ฟอร์ม+ admin_data.js ไป `web\` → `python build_pwa.py` → deploy (วิธี git หรือ gh api)
- ข้อมูลเก่าที่บันทึกไว้ก่อนรอบนี้ ในฟอร์มป้ายโหลดกลับได้ (fallback match ชื่อตำบลจากข้อมูลเก่า; ใหม่ใช้รหัส)
- ฟอร์มที่เหลือ (อาคาร/การใช้ประโยชน์×2/ถ่ายรูป) **ไม่มี dropdown จ-อ-ต** — ไม่ต้องแตะ (ผู้ใช้สั่งเฉพาะ 3 ฟอร์มนี้)
- server เก่า/Chrome เก่า: ทดสอบผ่าน CDP 9222 (Chrome PID 9424) + server.py port 8000 (PID 13856) — ยังรันอยู่