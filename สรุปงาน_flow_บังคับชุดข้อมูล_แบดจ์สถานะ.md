# สรุปงานระบบ LTAX Survey — รอบ B+C: ระบบ flow บังคับชุดข้อมูล + badge สถานะชุดบนหน้าแรก (19/08/2569)

> ขั้น B+C ต่อจากงาน A (popup "บันทึกข้อมูลเรียรอย ✓" commit `9b4f5e0`) ตามที่ผู้ใช้สั่ง "ทำเลย"
> **Deploy สำเร็จ: commit `e95b5b7` → https://noom25.github.io/ltax-survey/**

## ✅ B: ระบบ flow บังคับชุดข้อมูล (ต่อเนื่องในฟอร์มเดียวกัน)
- หลังบันทึกเจ้าของ → modal เดิมมีปุ่ม **"กรอกชุดถัดไป"** → เปิดฟอร์มถัดไป **ในฟอร์มเดิม (SPA ไม่ reload)**
  พร้อม **auto-fill ค่าข้ามฟอร์ม** ผ่าน `sessionStorage('ltax_next')` = ctx {psnId, fullName, parcelCode, buildingCode}
- ลำดับ flow: เจ้าของ → ที่ดิน → ใช้ที่ดิน → อาคาร → ใช้อาคาร → **(ถ้ามีป้าย)** ป้าย → (เจ้าของคนถัดไป)
- การทำงาน: `goNextForm(fid, ctx)` merge ctx เดิม+ใหม่ → เปิดฟอร์ม → `applyNextCtx()` อ่าน ctx แล้วเติมฟอร์มโดยไม่เขียนทับช่องที่ผู้ใช้กรอกแล้ว (เช็ค `window.__editKey`)
- ฟอร์มใช้อาคารบันทึก → modal เปลี่ยนปุ่มเป็น **"มีป้าย — กรอกป้าย"** + ปุ่ม **"ไม่มีป้าย (จบชุด)"** (กดแล้วกลับหน้าแรก) — ฟอร์มป้ายบันทึก → กลับไปเจ้าของคนถัดไป

## ✅ C: badge สถานะชุดบนหน้าแรก
- `chainState(psnId)` + `chainBadge(psnId)` แสดงบน:
  · **ผลค้นหา** (เฉพาะหมวด ltax_owner) + **หน้าดูข้อมูลเจ้าของ** (renderOwnerDetail)
- ระดับสถานะ (steps x/6): 0=ยังไม่มีที่ดิน / 1=ยังไม่ได้กรอกใช้ที่ดิน / 2=ยังไม่มีอาคาร / 3=ยังไม่ได้กรอกใช้อาคาร /
  **4=ครบ 5 หมวด (ไม่มีป้าย) ✓ / 5=ครบทั้ง 6 หมวด ✓**
- badge ข้อความ เช่น "▶ ยังไม่ได้กรอกสิ่งปลูกสร้าง (3/6)" / "✓ ครบ 5 หมวดแล้ว (ไม่มีป้าย) (5/6)" — สีเขียว=ครบ แดง/เหลือง=ไม่ครบ

## 🛠️ บั๊กที่เจอและแก้ระหว่างทำ (สำคัญ)
1. **hook auto-fill ฝังผิดฟอร์มใช้ที่ดิน** — ไปติดใน `clearUsageEntry()` แทน init (init เป็น statement-level)
   → ย้าย `setTimeout(applyNextCtx, 0)` ไปท้ายสุดของ script ฟอร์มใช้ที่ดิน
2. **`goNextForm(fid)` ตัวเก่า (รับ 1 พารามิเตอร์) ประกาศทีหลังทับตัวใหม่ (รับ ctx)** → ctx ไม่ถูกส่ง จึง auto-fill ว่างทั้ง flow
   → ลบตัวเก่าทุกฟอร์ม (patch_next_flow_v3.py: ลบ pattern ตัวเก่า → copy → build → verify 0 ตัวเก่าใน index.html)
3. **applyNextCtx ฟอร์มอาคารเติมเจ้าของ+ช่องผูกแต่ไม่ push `lands[]`** → บันทึกได้แต่ `รหัสแปลงที่ดิน` ว่าง → badge นับไม่ถึงระดับอาคาร
   → เพิ่ม `lands = [{code: c.parcelCode}]; renderLands();`
4. badge นับ step ผิด (แสดง (4/6) ทั้งที่ครบ 5 หมวด) → แก้เป็นนับหมวดจริง (level+1)/6

## 🧪 ทดสอบ (test_flow_bc.py — CDP 9222 + hard reload ล้าง SW cache เก่า)
| ขั้น | ผล |
|---|---|
| เจ้าของ → ที่ดิน | auto owners=[ชุด ทดสอบ 1234567890144] ✅ |
| ที่ดิน → ใช้ที่ดิน | auto psnId\|parcel=[1234567890144\|50B1] ✅ |
| ใช้ที่ดิน → อาคาร | owners+psnId+parcel ✅ |
| อาคาร → ใช้อาคาร | auto buildingCode=50B1-B1, psn ✅ |
| ใช้อาคาร | modal askSign "มีป้าย — กรอกป้าย" + ปุ่มไม่มีป้าย ✅ |
| ไม่มีป้าย | กลับหน้าแรก ✅ |
| ค้นหาเจ้าของ | badge "✓ ครบ 5 หมวดแล้ว (ไม่มีป้าย) (5/6)" ✅ (ทั้งผลค้นหา+หน้าดู) |
| เก็บข้อมูล | 1/1/1/1/1 ครบ, **JS errors: 0** ✅ |

## 🚀 Deploy
- commit `e95b5b7` (15 ไฟล์: admin_data.js, home.html, index.html, manifest.json, sw.js v20260819-231126, 6 ฟอร์ม, icons)
- ตรวจเว็บจริง (โปรไฟล์ใหม่ ไม่โดน SW เก่า): chainState/chainBadge=true, เปิดฟอร์ม+modal+ปุ่ม save ครบ ✅
- **หมายเหตุผู้ใช้:** บนมือถือ/เบราว์เซอร์ที่เคยติดตั้ง SW เก่า ต้อง hard refresh 1 ครั้ง (หรือล้าง cache) ถึงจะได้เวอร์ชันใหม่

## งานค้างถัดไป
- [ ] อัปเดต `คู่มือการใช้งาน.md` ให้ครอบคลุม PWA + หน้าถ่ายรูป + flow ชุดข้อมูล (B)
- [ ] บอทอัป LTAX Online จริง (ต้อง login+OTP ผู้ใช้อยู่หน้าจอ)