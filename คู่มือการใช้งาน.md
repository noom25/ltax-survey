# คู่มือการใช้งานระบบ LTAX สำรวจภาคสนาม → เข้า LTAX Online

**ระบบ v1** — สำรวจข้อมูลภาคสนามด้วยมือถือ แล้วส่งเข้าเครื่องคอมพิวเตอร์ เพื่อแปลงเป็นไฟล์
Excel + รูปภาพ และอัปเข้า **ระบบ LTAX Online** (https://lsso.dla.go.th) ด้วยบอทอัตโนมัติ

---

## สารบัญ

1. [ภาพรวมระบบ](#1-ภาพรวมระบบ)
2. [สิ่งที่ต้องเตรียม](#2-สิ่งที่ต้องเตรียม)
3. [โหมดการใช้งาน (2 แบบ)](#3-โหมดการใช้งาน-2-แบบ)
4. [ขั้นตอนที่ 1: เริ่ม server ที่คอมฯ](#4-ขั้นตอนที่-1-เริ่ม-server-ที่คอม)
5. [ขั้นตอนที่ 2: เปิดฟอร์มบนมือถือ](#5-ขั้นตอนที่-2-เปิดฟอร์มบนมือถือ)
6. [ขั้นตอนที่ 3: ลำดับการสำรวจแต่ละฟอร์ม](#6-ขั้นตอนที่-3-ลำดับการสำรวจแต่ละฟอร์ม)
7. [ขั้นตอนที่ 4: ส่งข้อมูลกลับคอมฯ](#7-ขั้นตอนที่-4-ส่งข้อมูลกลับคอม)
8. [ขั้นตอนที่ 5: ตรวจสอบไฟล์ที่ได้](#8-ขั้นตอนที่-5-ตรวจสอบไฟล์ที่ได้)
9. [ขั้นตอนที่ 6: อัปข้อมูลเข้าสู่ LTAX Online](#9-ขั้นตอนที่-6-อัปข้อมูลเข้าสู่-ltax-online)
10. [การใช้แบบพกไปสนาม (แอป Documents)](#10-การใช้แบบพกไปสนาม-แอป-documents)
11. [ค้นหา / แก้ไข / ลบข้อมูล](#11-ค้นหา--แก้ไข--ลบข้อมูล)
12. [การแก้ปัญหาที่พบบ่อย](#12-การแก้ปัญหาที่พบบ่อย)

---

## 1. ภาพรวมระบบ

```
┌─────────────┐      ┌──────────────────────────────┐      ┌────────────────┐
│  มือถือ/แท็บเล็ต  │      │         คอมพิวเตอร์             │      │    ระบบ LTAX    │
│ (iPhone/iPad/  │ WiFi │  bot_upload_data v1\         │      │     Online      │
│  Android)      │──────│                             │──────│ (กรมส่งเสริมฯ)  │
│  ฟอร์มกรอกข้อมูล│      │  web\           (หน้าเว็บ)     │      │                │
│  + ถ่ายรูป      │      │  server.py      (ตัวกลาง)     │      │                │
│  + บันทึก       │      │  ltax_data_all.json         │      │                │
│                │      │  json_to_excel.py (แปลง)     │      │                │
│                │      │  owner_land_...xlsx  ←───────┼──────│                │
│                │      │  photos\          (แยกรูป)    │      │                │
│                │      │  run_all.py  (บอทอัป)         │      │                │
└─────────────┘      └──────────────────────────────┘      └────────────────┘
```

**หลักการทำงาน:** มือถือใช้เป็นแค่หน้าจอกรอกข้อมูล → ข้อมูล+รูปถูกส่งกลับคอมฯ →
คอมฯ แปลงเป็น Excel + แยกรูป → บอทเปิดเบราว์เซอร์เข้าสู่ LTAX Online กรอกข้อมูลให้ครบ

---

## 2. สิ่งที่ต้องเตรียม

### ฝั่งคอมพิวเตอร์ (เครื่องที่ใช้เปิดบอท)
- Windows + มี **Python 3** ติดตั้งแล้ว
- โฟลเดอร์ `C:\LTAX_Automation\bot_upload_data v1\` พร้อมไฟล์ครบชุด
- ติดตั้ง library ที่จำเป็น:
  ```
  pip install pandas openpyxl selenium webdriver-manager pillow
  ```
- ติดตั้ง **Google Chrome** (บอทใช้ Chrome)

### ฝั่งมือถือ (เครื่องที่ใช้สำรวจ)
- iPhone/iPad/Android ต่อ **WiFi เดียวกันกับคอมฯ** (สำหรับโหมดใช้ server)
- หรือติดตั้งแอป **Documents (Readdle)** ฟรี (สำหรับโหมดพกไปสนาม — ดูข้อ 10)

### เครือข่าย
- คอมฯ และมือถือต้องอยู่ในเครือข่าย WiFi เดียวกัน
- เจอ `192.168.x.x` ในคำสั่ง `ipconfig` (คอมฯ Windows)

---

## 3. โหมดการใช้งาน (2 แบบ)

| โหมด | เหมาะกับ | ข้อดี |
|---|---|---|
| **A. ใช้ server (แนะนำ)** | สำรวจในเขตที่ WiFi เข้าถึงคอมฯ ได้ | กด "ส่งข้อมูลไปคอมฯ" ส่งกลับอัตโนมัติ ไม่ต้องโอนไฟล์เอง |
| **B. พกไปสนาม (แอป Documents)** | ออกพื้นที่ไกล ไม่มี WiFi | ใช้แอป Documents ทำ server ภายในเครื่องมือถือ แล้วค่อยโอน JSON กลับทีหลัง |

---

## 4. ขั้นตอนที่ 1: เริ่ม server ที่คอมฯ

1. เปิดโฟลเดอร์ `C:\LTAX_Automation\bot_upload_data v1\`
2. **ดับเบิลคลิกไฟล์ `start_server.bat`**
3. หน้าต่าง CMD จะแสดงที่อยู่ URL เช่น:
   ```
   เปิดบนมือถือได้ที่: http://192.168.1.50:8000
   ```
4. **เปิดหน้าต่างนี้ค้างไว้** ห้ามปิด (ปิดเมื่อเลิกใช้งาน)
5. ถ้าคอมฯ เปิด Firewall แล้วมือถือเข้าไม่ได้ → ให้วินโดวส์อนุญาต Python ผ่าน Firewall (จะมีหน้าต่างเด้งถามตอนรันครั้งแรก กด "อนุญาตการเข้าถึง")

> ⚠️ ถ้าต้องการเปลี่ยนหมายเลข port หรือมี port 8000 ซ้ำ ให้เปิด `server.py` แล้วแก้ตัวแปร `PORT`

---

## 5. ขั้นตอนที่ 2: เปิดฟอร์มบนมือถือ

1. มือถือต่อ WiFi **เดียวกันกับคอมฯ**
2. เปิด Safari (iPhone/iPad) หรือ Chrome (Android)
3. พิมพ์ URL ที่ได้จาก server เช่น `http://192.168.1.50:8000`
4. จะเจอหน้าแรก (index) มีปุ่มเข้าฟอร์มทั้ง 6 ฟอร์ม + ช่องค้นหา

> ⚠️ **ห้าม**เปิดไฟล์ `index.html` แบบ "เปิดไฟล์ตรง" จากแอป Files/File Manager
> เพราะมือถือจะไม่ยอมบันทึกข้อมูล (localStorage ถูกบล็อก) — **ต้องผ่าน URL http:// เท่านั้น**

---

## 6. ขั้นตอนที่ 3: ลำดับการสำรวจแต่ละฟอร์ม

ต้องสำรวจตามลำดับ (ระบบผูกข้อมูลข้ามฟอร์ม):

```
1. เจ้าของทรัพย์สิน  → 2. ที่ดิน  → 3. การใช้ประโยชน์ที่ดิน
                                  ↘ 4. สิ่งปลูกสร้าง → 5. การใช้ประโยชน์สิ่งปลูกสร้าง
                                  ↘ 6. ป้าย
```

### ฟอร์มที่ 1: เจ้าของทรัพย์สิน
- เลขบัตรประชาชน 13 หลัก, ชื่อ-นามสกุล, ที่อยู่ตามบัตร และที่อยู่ปัจจุบัน
- กดปุ่ม **"บันทึก"** → ข้อมูลถูกเก็บในเครื่อง (localStorage) + ดาวน์โหลด JSON รายแปลง
- ใช้เลขบัตรนี้เป็นตัวกลางผูกกับที่ดิน/อาคาร/ป้าย

### ฟอร์มที่ 2: ที่ดิน (รหัสแปลง + โฉนด)
- **รหัสแปลงที่ดิน** (กำหนดเอง เช่น `50A001`) — ใช้เป็นตัวอ้างอิงผูกกับข้อมูลอื่น
- ข้อมูลโฉนด: ประเภทเอกสาร, เลขที่เอกสาร, หน้าสำรวจ, เล่ม, ระวาง, เลขที่ดิน
- เนื้อที่ (ไร่-งาน-ตร.วา) → ระบบคำนวณพื้นที่รวม/ราคาประเมินให้อัตโนมัติ
- เลือกเจ้าของ (เลขบัตร) จากที่เพิ่มไว้
- **รูปที่ดิน:** 1 รูป (ถ้าจำเป็น) — ระบบบีบอัดให้อัตโนมัติ

### ฟอร์มที่ 3: การใช้ประโยชน์ที่ดิน
- เลือกแปลงที่ดิน → เพิ่มรายการการใช้ประโยชน์ (เช่น นาปี, ไร่นา ฯลฯ)
- ระบุเนื้อที่ที่ใช้ และผู้ทำประโยชน์

### ฟอร์มที่ 4: สิ่งปลูกสร้าง (อาคาร/บ้าน)
- รหัสสิ่งปลูกสร้าง (เช่น `50A001-B001`) — ระบบมีปุ่มสร้างรหัสอัตโนมัติ
- เลือกแปลงที่ดิน + เจ้าของ
- ลักษณะอาคาร, ขนาด, ราคาประเมิน
- **รูปอาคาร: 1 รูป** (LTAX Online รับแปลงละ 1 รูป)

### ฟอร์มที่ 5: การใช้ประโยชน์สิ่งปลูกสร้าง
- เลือกอาคาร → เพิ่มรายการการใช้ประโยชน์ (เช่น อยู่อาศัย, ร้านค้า)

### ฟอร์มที่ 6: ป้าย
- รหัสป้าย (เช่น `50A001-P001`), ชื่อสถานประกอบการ, เจ้าของ, อาคารที่ติดตั้ง
- ขนาดป้าย (กว้าง × ยาว ซม.) → คำนวณภาษีให้อัตโนมัติ
- **รูปป้าย: 1 รูป**

### ข้อควรจำการกรอก
- กด **บันทึก** ทุกครั้งหลังกรอกฟอร์ม
- กลับหน้าแรกทุกครั้งเพื่อดู summary (จำนวนรายการแต่ละประเภท) ว่าครบหรือยัง
- ข้อมูลทั้งหมดถูกเก็บ **ในเครื่องมือถือ** จนกว่าจะกดส่งกลับคอมฯ

---

## 7. ขั้นตอนที่ 4: ส่งข้อมูลกลับคอมฯ

เมื่อสำรวจครบแล้ว กลับไปหน้าแรก (index):

1. ตรวจ summary ว่าจำนวนเจ้าของ/ที่ดิน/อาคาร/ป้าย ครบ
2. กดปุ่ม **"ส่งข้อมูลไปคอมฯ"**
3. จะมีข้อความยืนยัน:
   ```
   ส่งข้อมูลไปคอมฯ สำเร็จ
   เจ้าของ 3 | ที่ดิน 3 | ใช้ที่ดิน 3 | อาคาร 2 | ใช้อาคาร 2 | ป้าย 1
   สร้าง Excel: owner_land_building_template.xlsx
   แยกรูป: 6 รูป (ใหม่ 6)
   ```
4. ข้อมูลถูกเขียนทับไฟล์ล่าสุดบนคอมฯ **ทุกครั้งที่ส่ง** (ครั้งล่าสุดชนะ)

> ⚠️ ปุ่ม "ส่งข้อมูลไปคอมฯ" ใช้ได้เฉพาะตอนเข้าผ่าน server (`http://...:8000`)
> ถ้าเปิดแบบพกสนาม (แอป Documents) ให้ใช้ปุ่ม **"รวมข้อมูล (JSON)"** แทน — ดูข้อ 10

---

## 8. ขั้นตอนที่ 5: ตรวจสอบไฟล์ที่ได้

หลังส่งข้อมูล ภายในโฟลเดอร์ v1 จะมีไฟล์ (สร้าง/อัปเดตอัตโนมัติ):

```
bot_upload_data v1\
├── ltax_data_all.json                    ← ข้อมูลดิบ + รูป (base64) ทั้งหมด
├── owner_land_building_template.xlsx     ← Excel ที่บอทอ่าน (6 ชีต)
└── photos\
    ├── 50A001-B001_1.jpg                 ← รูปอาคาร (ชื่อตามรหัสแปลง)
    ├── 50A001-P001_1.jpg                 ← รูปป้าย
    └── ...
```

**ตรวจด้วยตัวเอง:**
- เปิด `owner_land_building_template.xlsx` ด้วย Excel
- ดูว่าแต่ละชีตมีจำนวนแถวตรงกับข้อมูลที่กรอก
- เปิดคอลัมน์ `Path รูปภาพ` → ต้องชี้ไปที่ไฟล์ใน `photos\` ที่มีอยู่จริง

> ถ้าพบว่ารูปผิดแปลง / ข้อมูลไม่ครบ → กลับไปแก้บนมือถือ (หน้าแรก→ค้นหา→แก้ไข) แล้วส่งใหม่

---

## 9. ขั้นตอนที่ 6: อัปข้อมูลเข้าสู่ LTAX Online

### 9.1 ตั้งค่าล็อกอิน (ครั้งแรกเท่านั้น)
เปิด `config.json` แล้วตรวจ:
```json
"online_user": "3330500109674",
"online_pass": "รหัสผ่าน",
"ltax_url":   "https://lsso.dla.go.th/login.do"
```

### 9.2 เลือกโหมด OTP
ระบบมี 2 โหมด:
- **Auto OTP** — รอรับ OTP ผ่าน Gmail (ต้องตั้งค่า `credentials.json`/`token.json` จาก Google API)
- **Manual OTP** — ให้กรอก OTP เองที่หน้าต่างเบราว์เซอร์ (แนะนำถ้ายังไม่ได้ตั้ง Gmail API)

### 9.3 รันบอท
เปิด CMD ในโฟลเดอร์ v1 แล้วรัน:

```bash
# แบบอัตโนมัติ (ใช้ Excel ที่ server สร้าง)
python run_all.py --excel owner_land_building_template.xlsx

# แบบกรอก OTP เอง
python run_all.py --excel owner_land_building_template.xlsx --manual-otp
```

> ⚠️ **สำคัญ:** ระบุ `--excel owner_land_building_template.xlsx` เสมอ
> (ค่า default ของ run_all.py เป็น `..._v2.xlsx` ซึ่งคนละไฟล์กับที่ server สร้าง)

บอทจะ:
1. เปิด Chrome → ล็อกอิน LTAX Online
2. กรอกเจ้าของ → ที่ดิน → การใช้ที่ดิน → สิ่งปลูกสร้าง → การใช้สิ่งปลูกสร้าง → ป้าย ตามลำดับ
3. อัปโหลดรูปจาก `photos\` ที่ผูกกับรหัสแปลง
4. แสดงสรุปผลสำเร็จ/ล้มเหลว และบันทึก log ไฟล์ `*_done.txt`

### 9.4 ตรวจผล
- หน้าจอสรุปท้ายสุด: `👤 Owner: สำเร็จ 3 ล้มเหลว 0` ...
- ถ้ามีรายการล้มเหลว → ดูไฟล์ `bot_log.txt` และ `error_log.txt` + screenshot ในโฟลเดอร์ `screenshots\`
- ลบรายการที่ล้มเหลวออกจาก `*_done.txt` แล้วรันใหม่ (บอทจะข้ามรายการที่สำเร็จไปแล้ว)

---

## 10. การใช้แบบพกไปสนาม (แอป Documents)

กรณีออกพื้นที่ไกล ไม่มี WiFi ถึงคอมฯ:

### เตรียม
1. โฟลเดอร์ `mobile_offline\` มีไฟล์ HTML 7 ไฟล์ + วิธีใช้.txt
2. Zip เป็น `mobile_offline.zip` → โอนเข้าโทรศัพท์ (AirDrop/Line/USB)

### เปิดใช้งานบนมือถือ
1. ติดตั้งแอป **Documents** (Readdle) ฟรี จาก App Store
2. แตกไฟล์ zip ในแอป
3. แตะ `index.html` → แอปเปิดเป็น server ภายในเครื่อง (`http://localhost`)
4. กรอกข้อมูล/ถ่ายรูป/บันทึกตามปกติ (ข้อมูลเก็บในเครื่อง)

### กลับมาโอนข้อมูลเข้าคอมฯ
1. บนมือถือ หน้าแรก กด **"รวมข้อมูล (JSON)"** → ได้ไฟล์ `ltax_data_all.json`
2. โอนไฟล์นี้กลับคอมฯ วางที่ `C:\LTAX_Automation\bot_upload_data v1\`
3. ที่คอมฯ รัน:
   ```
   python json_to_excel.py --input ltax_data_all.json --output owner_land_building_template.xlsx
   ```
4. ได้ Excel + `photos\` ครบ → ไปขั้นตอนที่ 9 (รันบอท)

> ⚠️ ในโหมดนี้ปุ่ม "ส่งข้อมูลไปคอมฯ" ไม่ทำงาน — ต้องใช้ปุ่ม "รวมข้อมูล (JSON)" แล้วโอนไฟล์เอง

---

## 11. ค้นหา / แก้ไข / ลบข้อมูล

ที่หน้าแรก (index) มีช่องค้นหา:

1. พิมพ์เลขบัตร / รหัสแปลง / ชื่อ เพื่อค้นหา → ขึ้นผลลัพธ์ทุกหมวดที่ตรง
2. กด **"ดู / แก้ไข"** → ฟอร์มเปิดพร้อมข้อมูลเดิม → แก้ไข → บันทึก (ทับรายการเดิม ไม่ซ้ำ)
3. ที่หน้าแรก กด **"ล้างข้อมูลทั้งหมด"** เพื่อล้าง localStorage ทั้งหมดในเครื่องมือถือ

---

## 12. การแก้ปัญหาที่พบบ่อย

| ปัญหา | สาเหตุ / วิธีแก้ |
|---|---|
| มือถือเข้า URL ไม่ได้ | เช็คว่าต่อ WiFi เดียวกัน / เปิด Firewall ให้ Python / รัน `ipconfig` เช็ค IP ใหม่ |
| กรอกข้อมูลแล้วกดบันทึกแต่ข้อมูลหาย | เปิดผ่าน `file://` (ไฟล์ตรง) — **ต้องเปิดผ่าน `http://` เสมอ** |
| alert "พื้นที่เก็บข้อมูล (localStorage) เต็ม" | localStorage มีจำกัด (~5MB) — ลดขนาดรูป / ลบรายการเก่า / กดส่งข้อมูลบ่อย ๆ |
| ปุ่ม "ส่งข้อมูลไปคอมฯ" ไม่ขึ้นผล | server ถูกปิด หรือเข้าผ่านโหมด Documents — ใช้ปุ่ม "รวมข้อมูล (JSON)" แทน |
| ไฟล์รูปหายจาก photos\ | ตรวจว่าใน JSON มี `รูปภาพ_data`/`รูปที่ดิน_data` (ฝังรูป) — ต้องเลือก รูปจริงในฟอร์ม ไม่ใช่แค่กรอก Path |
| บอท login ไม่ผ่าน | ตรวจ user/pass ใน config.json / ใช้ `--manual-otp` / OTP หมดอายุ → รันใหม่ |
| บอทบอกไม่พบ Excel | ต้องใช้ `--excel owner_land_building_template.xlsx` (ชื่อที่ server สร้าง) |
| อยากสำรวจใหม่ทั้งชุด | หน้าแรก กด "ล้างข้อมูลทั้งหมด" แล้วกรอกใหม่ |
| อยากใช้หลายแปลงต่อวัน | ส่งข้อมูลเข้าคอมฯ ได้บ่อยเท่าที่ต้องการ — ไฟล์จะถูกเขียนทับเวอร์ชันล่าสุด |

---

## ข้อมูลทางเทคนิค (สำหรับผู้ดูแล)

| ไฟล์ | หน้าที่ |
|---|---|
| `web\` | ฟอร์ม HTML 7 ไฟล์ (ไม่มี dependency ภายนอก) — ฝัง CSS/JS หมดแล้ว |
| `server.py` | serve `web\` + API `/api/upload` รับ JSON → เรียก `json_to_excel.py` อัตโนมัติ |
| `start_server.bat` | ตัวเปิด server ง่าย ๆ |
| `json_to_excel.py` | แปลง JSON → Excel 6 ชีต + แยกรูป HEIC→JPG ไป `photos\` |
| `run_all.py` | บอทหลัก: login → owner → land → building → sign (อ่าน Excel) |
| `bot_add_*.py` | บอทย่อยแต่ละประเภท (run_all เรียกใช้งาน) |
| `otp_manager_v2.py` | จัดการ OTP (อ่านจาก Gmail/ไฟล์) |
| `config.json` | ตั้งค่า: URL, user/pass, โหมด OTP, ข้อมูลอ้างอิง |
| `field_mapping_complete.json` | mapping ชื่อคอลัมน์ Excel ↔ ฟิลด์เว็บ LTAX |
| `photos\` | รูปที่แยกจาก JSON (ชื่อตามรหัสแปลง) |
| `ltax_data_all.json` | ข้อมูลดิบรวมทั้งหมด (JSON + รูป base64) |
| `mobile_offline\` | ฟอร์มชุดพกสนาม (ใช้กับแอป Documents) |

---

*เอกสารนี้จัดทำสำหรับระบบ bot_upload_data v1 — อัปเดตล่าสุดเมื่อจัดทำคู่มือ*
