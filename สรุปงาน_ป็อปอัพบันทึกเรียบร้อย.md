# สรุปงานระบบ LTAX Survey — รอบ popup "บันทึกข้อมูลเรียบร้อย ✓" (19/08/2569)

> งานขั้น A ตามที่ผู้ใช้เสนอ (จากปัญหา: กดบันทึกแล้วเงียบ ไม่รู้ว่าบันทึกไปหรือยัง)
> ขั้น B (บังคับ flow ชุดข้อมูล) และ C (สถานะชุดบนหน้าแรก) — ค้างไว้รอรอบถัดไป

---

## 1. งานที่ทำรอบนี้ (หลัก)

### ✅ popup ยืนยันการบันทึกทุกฟอร์ม (6 ฟอร์ม)
- **ปัญหาที่เจอ:** ทุกฟอร์มกดบันทึกแล้ว `window.__saved=true` + แสดง preview เท่านั้น **ไม่มี popup** — ผู้ใช้ไม่รู้ว่าบันทึกไปแล้ว
- **วิธีแก้:** ฝัง modal `#ltaxModal` (theme กรม: หัวน้ำเงินไล่ `#367fa9→#1b4f8a` + วงกลมเขียวติ๊ก ✓ + การ์ดขาวโค้ง) ให้ทุกฟอร์ม + ฟังก์ชัน `showSavedModal(info, next)`
- **เรียกใช้หลัง `LTAX_store(...)` ในทุกฟอร์ม (แทรกด้วยสคริปต์ `insert_modal.py`):**
  | ฟอร์ม | ข้อความใน popup | ปุ่มถัดไป |
  |---|---|---|
  | เจ้าของ | เลขบัตรประชาชน: xxx | → ฟอร์มที่ดิน |
  | ที่ดิน | รหัสแปลงที่ดิน: xxx (ตำบล …) | → ใช้ประโยชน์ที่ดิน |
  | ใช้ประโยชน์ที่ดิน | รหัสแปลง + จำนวนรายการใช้ | → สิ่งปลูกสร้าง |
  | สิ่งปลูกสร้าง | รหัส + (แปลง …) | → ใช้ประโยชน์อาคาร |
  | ใช้ประโยชน์อาคาร | รหัส + จำนวนรายการใช้ | → ป้าย |
  | ป้าย | รหัสป้าย + จังหวัด | → เจ้าของคนถัดไป (วนชุดใหม่) |
- **ปุ่มใน popup (4 ปุ่ม):**
  - **กรอกชุดถัดไป (ต่อไป)** — เปิดฟอร์มถัดไปตามลำดับ (ป้าย = ปุ่มเปลี่ยนเป็น "กรอกข้อมูลเจ้าของรายถัดไป")
  - **เพิ่มรายการใหม่** — resetForm() เดิม (มี confirm "ล้างข้อมูลในฟอร์ม?" เหมือนเดิม)
  - **กลับหน้าแรก** — `window.__goHome()` (SPA) / index.html
  - **ปิด** — ซ่อน popup (ข้อมูลบันทึกแล้ว อยู่หน้าเดิม)
- ฟังก์ชันนำทาง `goNextForm(fid)` — ใช้ `window.openForm` ในโหมด SPA, fallback เปิดไฟล์ฟอร์มแยก

### ✅ ทดสอบจริงผ่าน (Selenium CDP 9222)
| เคส | ผล |
|---|---|
| บันทึกเจ้าของ (กรอกครบ) → modal เด้ง ✓ ขึ้นข้อความเลขบัตรฯ | ✅ |
| กด "กรอกชุดถัดไป" → ฟอร์มที่ดินเปิด | ✅ |
| บันทึกเจ้าของ → กด "กลับหน้าแรก" → กลับ home | ✅ |
| บันทึกที่ดิน → "เพิ่มรายการใหม่" → confirm ล้าง → ฟอร์มว่าง | ✅ |

### ✅ Deploy GitHub Pages
- commit `9b4f5e0` (15 ไฟล์) → https://noom25.github.io/ltax-survey/
- ตรวจเว็บจริง: index.html มี ltaxModal 6 จุด + showSavedModal 6 ฟังก์ชัน + "กรอกชุดถัดไป" ✓

---

## 2. งานค้าง (รอรอบถัดไป — ตกลงกับผู้ใช้แล้ว)
- [ ] **B: บังคับ flow ชุดข้อมูล** — หลังบันทึกเจ้าของต้องกรอกต่อตามลำดับ:
      เจ้าของ → ที่ดิน → ใช้ที่ดิน → สิ่งปลูกสร้าง → ใช้อาคาร → ป้าย (ถ้ามี)
      (โหลดเลขบัตร/รหัสแปลง/รหัสอาคาร ให้อัตโนมัติข้ามฟอร์ม + modal ถาม "มีที่ดินไหม/มีป้ายไหม")
- [ ] **C: สถานะชุดบนหน้าแรก** — แสดง badge ต่อเจ้าของ เช่น "ชุดไม่ครบ (กรอกที่ดินต่อ)" / ✓ ครบทั้ง 6 —
      ข้อมูลผูกกันด้วยรหัสอยู่แล้ว (parcelCode/buildingCode/signboardCode + เลขบัตร) เห็นชัดตอนดู/รวม Excel

---

## 3. ไฟล์ที่แก้/สร้าง รอบนี้

| ไฟล์ | การเปลี่ยนแปลง |
|---|---|
| 6 ฟอร์ม (root) | ฝัง modal `#ltaxModal` + `showSavedModal/hideSavedModal/goNextForm` + call หลัง LTAX_store |
| `web\` (index.html 277,382 B, sw.js v20260819-222745) | build + deploy (commit 9b4f5e0) |
| `C:\Users\NOOM\AppData\Local\Temp\opencode\insert_modal.py` | สคริปต์ฝัง modal อัตโนมัติ (6 ฟอร์ม, กันแก้ซ้ำ) |
| `C:\Users\NOOM\AppData\Local\Temp\opencode\test_modal.py` | ทดสอบ 3 เคสผ่าน |

---

## 4. ข้อควรจำ
- **id ซ้ำข้อควรรู้:** ใน index.html template แต่ละฟอร์มมี `#ltaxModal` (6 ตัว) — เปิดทีละฟอร์ม
  ใช้ตัวแรกของ DOM (getElementById) → ทำงานถูกต้อง (ทดสอบผ่าน) แต่ถ้าเจอปัญหาแปลก ๆ เรื่อง modal จำไว้จุดนี้ด้วย
- **resetForm เดิมมี confirm** "ล้างข้อมูลในฟอร์ม?" — กด "เพิ่มรายการใหม่" จะเจอ confirm 1 ที (ปกติ)
- ฟอร์ม นับเฉพาะ 6 ฟอร์มกรอกข้อมูล (home/ถ่ายรูป ไม่มี modal)